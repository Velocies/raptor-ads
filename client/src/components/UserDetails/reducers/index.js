const initialState = {
  isFetching: false,
  currentUserDetails: {
    createdAt: '',
  },
};


export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
