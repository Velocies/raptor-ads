const initialState = {
  signupForm: {
    firstName: '',
    email: '',
    lastName: '',
    businessName: '',
  },
};
export const profile = (state=initialState, action) => {
  switch(action.type) {
    case 'UPDATE_FORM_FIELD':
      return
  }
};
